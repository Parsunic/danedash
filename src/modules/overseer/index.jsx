// Overseer route entry — the Phosphor Terminal (full rebuild of the old chat page).
// Kept as a thin re-export so App.jsx's route wiring never changes.
export { default } from './OverseerTerminal.jsx'
